# Applied automatically for repos under ~/projects/argo/ (see gitconfig includeIf).
# Replace the placeholder email after install.
[user]
	email = dmitriy.finko@argomedia.co
[core]
	sshCommand = ssh -i ~/.ssh/argo
